FROM node:20-alpine
RUN apk add --no-cache wget
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN mkdir -p uploads/photos uploads/plugins uploads/branding
EXPOSE 5000
CMD ["node", "src/start.js"]
